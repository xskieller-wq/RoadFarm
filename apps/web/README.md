# RouteFarm Web (Next.js)

Phase 1 web application code currently lives at the **repository root** (`/src`, `/public`) while the monorepo is introduced.

This folder documents the target location for a future move:

```bash
# Planned layout
apps/web/
  src/
  public/
  next.config.ts
  package.json
```

Run the app from the repo root today:

```bash
npm install
npm run dev
```
